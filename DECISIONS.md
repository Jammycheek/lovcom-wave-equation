# Decision record

## Frozen decisions

1. The primary observation is a four-channel simplex: `D/S/C/P`. The former indirect channel is removed.
2. Masking is an attribute, not a channel.
3. Direct intimacy is not relationship approach. Direction is coded separately.
4. No interaction is a gap, not `p_D = 0`.
5. Character belief and analyst posterior are distinct objects.
6. Latent variables (`psi`, `h`, `U`) are inferred, never directly annotated.
7. Validation compares predictions with future blind-coded observations, not inferred latent states with subjective “ground truth”.
8. Interaction time is the default clock for cross-media comparison.
9. The frozen minimal validation target is `p_D`; a unique four-channel value function is not yet specified.
10. External forcing discovered after seeing a subject may not be inserted into the frozen differential equation. Event-triggered jump tests are used instead.
11. Network coupling is a v2.1 candidate and is considered only after shared and endogenous interventions fail.
12. Failures remain visible; they are not rewritten as successes.
13. Primary holdout scoring is open-loop. Holdout observations do not update states or parameters.
14. `J_n^TV` denotes an IE-to-IE Total Variation jump; `P_switch(W)` denotes the Tension Bridge squared-L2 switching metric.
15. Pair-external information updates are event-layer records, not dyadic IEs.
16. Tension Bridge v0.1 freezes the concept and metric; the wired confirmatory design remains inactive until a separately versioned `T_obs`/`D_obs` discriminant-validity pilot specification and passing decision are committed.
17. The first eligible tag is `v2.0-spec-freeze`, and only after the scoring reference implementation and independently preregistered recovery acceptance criteria pass on untouched validation data. Those numeric criteria are not yet approved; the existing benchmark is `NOT_ASSESSED`, never a retrospective pass. See `docs/NUMERICAL_RECOVERY_ACCEPTANCE_PLAN_v0.1.md`.
18. Prospective-control eligibility is determined by information exposure, not publication date. For Samuwan Herutsu, the user-declared last-read boundary is the end of comic volume 3 at the 2026-09-21 freeze; later content known before freeze is contaminated, and the exact first prospective chapter remains unconfirmed until its source-unit identifier is verified.
19. The Tension Bridge confirmatory experiment uses mechanically derived non-overlapping five-IE windows, future-blind raters with timestamp audit, deterministic question-order randomization, within-work-centered repeated split-half reliability, and Leave-One-Work-Out predictive validation. Human rating data remain uncollected.
20. A Static Tension Challenge failure overrides the final Bridge verdict while preserving the regression-only verdict as a diagnostic.
21. Subject 6 exposure labels and Direction outcomes are frozen in distinct commits and joined only after an exact-ID and chronology audit; the confirmatory corpus is capped at the first 10 qualified IEs per pair.
22. The Tension Bridge discriminant pilot passes only when the within-work-centered `|r(T,D)|` point estimate and deterministic work-cluster-bootstrap 95% upper bound are both below `.85`; pilot works and raters are excluded from confirmation by ID/hash comparison.
23. An optimizer start is successful only when L-BFGS-B reports success and the projected-gradient infinity norm is at most `1e-4`; invalid ODE trials use a finite quadratic penalty rather than a flat extreme sentinel.
24. Decision 23 describes the historical v0.1 rule, not a runtime-independent acceptance gate. Preserve its artifacts, pin the canonical family, and report build provenance and a separate three-state sensitivity annotation. Do not retune on any of the 40 exposed cases. N-2/N-5 remain open.
25. A pilot's exact works, windows and pseudonymous rater IDs are fixed before ratings. No sample enlargement after a verdict; no replacement of a failed pilot under the same instrument. Confirmation checks the complete declared result history against one plan and the entire v1.1 form hash. Hashes verify submitted evidence, not an operator's completeness claim.
26. T/D pilot v0.3 freezes a single recruitment cutoff before ratings. Each planned rater/window cell is either an actual answer or explicit blank-valued `NONRESPONSE`; late answers remain in raw data but do not enter primary analysis. A shortfall or cancellation is a completed, nonpassing result and cannot be replaced under the same instrument version. Before first rating, the plan hash needs a durable external timestamped registration; no such registration exists yet.
27. The `1e-8` per-IE forecast-mean tolerance applies only to fixed-parameter solver substitution. The historical use of that number for independently refitted paths is retained as an audit diagnostic, not a future acceptance rule. N-2/N-5 require separate development-block calibration and prior approval, including a cross-runtime policy.
